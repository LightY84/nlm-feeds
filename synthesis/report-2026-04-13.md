# NLM Socratic Agent — Ciclo #3

**Data:** 2026-04-13 | **Notebook interrogati:** 4 | **Domande totali:** 12 (3 × 4)

## Nuovi insight

### Security & Hacking
- **5 pattern di attacco composito documentati** per il 2025-2026: PRISMEX/APT28 (supply chain + zero-day weaponization), CVE-2026-20965 (cloud identity hijack + lateral movement), CPUID (API compromise + download poisoning), Storm-2755 (account hijack + payroll diversion)
- **5 gap compositi identificati**: AI-assisted phishing + session hijack, supply chain + smart contract poisoning, WASM bot evasion, deepfake vishing + MFA reset, GPU bit-flips + cloud escape
- **Principio chiave**: Zero Trust Esteso agli Agenti Non Umani — ogni bot deve avere identità verificabile

### Programming & Architecture
- **Pattern concreti per SPA Vanilla JS 10K+ LOC**: Proxy Reattivo con subscription per-key, Event Delegation centralizzata, Lazy Loading con import() dinamico
- **CQRS Frontend-Backend**: Commands via Mediator, Queries via Unified Data Agent su vista materializzata
- **Principio chiave**: Standard Codificati in "Golden Paths" — l'architettura viene incorporata nel codice, non documentata separatamente

### Tecnologie Abilitanti IT
- **GitOps single-container**: Blue-Green via staging Space parallelo, rollback automatico via health check post-deploy
- **Osservabilità a costo zero**: Health checks evoluti, structured logging JSON, telemetry in-memory Prometheus-style
- **Principio chiave**: Governance come Telemetria Operativa (GitOps 2.0) — conformità dimostrabile continuamente

### AI & System Design
- **Relational-Agentic RAG**: Text-to-SQL via Claude Tool-Use, NO vector store massivo — adatto a 5000 carte su 2GB RAM
- **Multi-modal via metadati**: estrarre descrizioni testuali dalle immagini, non indicizzare pixel
- **Principio chiave**: Dall'Assistenza all'Autonomia Agentica — agenti goal-oriented che imparano on-the-job (ALTK-Evolve)

## Gap più critico
**Relational-Agentic RAG per Pokefinance** — il pattern Text-to-SQL via MCP è direttamente implementabile e sblocca query naturali su prezzi, trend e pop report. Prerequisito: completare backfill tcggo_id ed espandere intenti GenUI Tier 2 per PSA/BGS.

## Principio trasversale emergente (cross-notebook)
Tutti e 4 i notebook convergono su: **"Architettura come Contesto Leggibile dalle Macchine"** — il paradigma 2026 richiede che policy, vincoli e standard siano codificati programmaticamente (pipeline, MCP tools, ADR machine-readable) per agenti AI autonomi.

## Note operative
- **Git push fallito** per lock file nel sandbox — commit locale salvato (`fc11911`). Richiede push manuale:
  ```bash
  cd ~/Documents/Claude/Projects/notebooklm/nlm-feeds
  git pull --rebase && git push
  ```
- 2 timeout NLM su 14 chiamate (retry riusciti con domande più brevi)
- Session IDs: `2fd59de5` (security), `519db091` (programming), `80c8b705` (tecnologie), `4019a1bc` (ai)

## Prossimo ciclo
Domenica 2026-04-20, 20:00
